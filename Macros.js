let TOP_BAR_HEIGHT = 50;
let SIDE_BAR_WIDTH = 250;

let LineTypeEnum = {
    DEFAULT: "DEFAULT",
	AXIS: "AXIS",
	AXIS_ARROW_HEAD: "AXIS_ARROW_HEAD",
	DEMAND: "DEMAND",
    SUPPLY: "SUPPLY",
	X_LABEL: "X_LABEL", //For lines pointing to a label on the x-axis
	Y_LABEL: "Y_LABEL", //For lines pointing to a label on the y-axis
};

let LabelTypeEnum = {
	DEFAULT: "DEFAULT",
	AXIS: "AXIS",
	LINE: "LINE",
	VALUE: "VALUE",
	CUSTOM: "CUSTOM",
}

let DEMAND_SUPPLY_GRAPH_MARGIN = 75;